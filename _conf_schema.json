{
  "arena_base": {
    "description": "棋擂台平台地址",
    "type": "string",
    "hint": "平台 HTTP/SSE Base URL，不要以 / 结尾",
    "default": "https://gulu624.icu"
  },
  "arena_fallback_bases": {
    "description": "备用平台地址",
    "type": "string",
    "hint": "备用平台地址；多个地址用英文逗号分隔。默认留空，不在公开插件里暴露服务器 IP。",
    "default": ""
  },
  "token": {
    "description": "Bot 接入 Token，留空则自动注册；也可在 WebUI 手动修改",
    "type": "string",
    "hint": "自动注册成功后插件会尽量写回 runtime config；日志不会输出完整 token",
    "default": ""
  },
  "auto_register": {
    "description": "Token 为空时自动注册",
    "type": "bool",
    "hint": "启动时 POST /api/bots/register 获取 token 并尝试保存到 AstrBot runtime config",
    "default": true
  },
  "commentary_enabled": {
    "description": "走棋时生成台词",
    "type": "bool",
    "default": true
  },
  "commentary_timeout_sec": {
    "description": "台词生成超时秒数",
    "type": "int",
    "hint": "若可用 LLM provider，会在此超时内尝试生成；失败则使用本地模板，不影响走棋",
    "default": 8
  },
  "llm_provider_mode": {
    "description": "LLM模型选择方式",
    "type": "string",
    "hint": "用于生成下棋对话和其它非象棋引擎 LLM 能力。default=使用 AstrBot 当前默认对话模型；custom=使用下面手动填写的 Provider ID。",
    "default": "default",
    "options": [
      "default",
      "custom"
    ]
  },
  "llm_provider_id": {
    "description": "手动指定 Provider ID",
    "type": "string",
    "hint": "仅当 LLM模型选择方式=custom 时生效。填写 AstrBot 模型/Provider 的 ID；留空或不可用会自动回退默认对话模型。",
    "default": ""
  },
  "auto_accept_challenges": {
    "description": "自动接受挑战",
    "type": "bool",
    "hint": "收到 challenge_received 事件后自动 POST /api/challenges/{id}/accept",
    "default": true
  },
  "engine_mode": {
    "description": "走棋模式",
    "type": "string",
    "hint": "auto=按自定义/本地/服务器/随机顺序回退；server_xqwlight=调用棋擂台 /api/analyze；local_xqwlight=调用本地 engine/analyze.js；custom_command/custom_http=自定义引擎；random=随机。旧 xqwlight 配置兼容为 server_xqwlight。",
    "default": "auto",
    "options": [
      "auto",
      "server_xqwlight",
      "local_xqwlight",
      "custom_command",
      "custom_http",
      "random"
    ]
  },
  "engine_depth": {
    "description": "引擎搜索深度",
    "type": "int",
    "hint": "传给 xqwlight/自定义引擎；建议 1-6，越大越慢。",
    "default": 3
  },
  "engine_timeout_sec": {
    "description": "引擎超时秒数",
    "type": "int",
    "hint": "本地命令、自定义命令、自定义 HTTP、服务器引擎的单次调用超时；超时会回退下一引擎，不影响走棋。",
    "default": 8
  },
  "custom_engine_command": {
    "description": "自定义命令行引擎",
    "type": "string",
    "hint": "命令需输出 JSON，如 {\"best_move\":\"h2e2\"}。默认从 stdin 读取统一 JSON；也可在命令中使用 {fen_json} 占位符作为参数。",
    "default": ""
  },
  "custom_engine_http_url": {
    "description": "自定义 HTTP 引擎 URL",
    "type": "string",
    "hint": "插件会 POST 统一 JSON 到该 URL，响应需为 {\"best_move\":\"...\"}；返回走法必须在 legal_moves 中。",
    "default": ""
  },
  "custom_engine_http_headers": {
    "description": "自定义 HTTP 引擎 Headers",
    "type": "text",
    "hint": "可选 JSON object，例如 {\"Authorization\":\"Bearer xxx\"}。格式错误会忽略。",
    "default": ""
  },
  "local_engine_node_path": {
    "description": "本地 xqwlight Node 路径",
    "type": "string",
    "hint": "local_xqwlight 调用 engine/analyze.js 时使用的 node 可执行文件路径或命令名。",
    "default": "node"
  },
  "move_timeout_sec": {
    "description": "走法提交超时秒数",
    "type": "int",
    "hint": "POST /api/matches/{match_id}/move 的超时时间",
    "default": 10
  },
  "verbose_logging": {
    "description": "输出详细运行日志",
    "type": "bool",
    "hint": "默认关闭：SSE连接、收到事件、选步、提交走法等日常日志降为 DEBUG，减少 AstrBot 控制台刷屏；排查问题时再开启。",
    "default": false
  },
  "announce_to_current_chat": {
    "description": "向当前聊天播报",
    "type": "bool",
    "hint": "预留字段；默认仅记录日志和响应命令，不主动向聊天发送事件",
    "default": false
  }
}
